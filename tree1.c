/* Given a binary tree and a node in the tree, returns the list of nodes at a 
 * distance n from that node
 */

#include <stdio.h>
#include <stdlib.h>

typedef struct node* NODE;
struct node {
    NODE right;
    NODE left;
    int val
};

int main () {
    NODE node1 = (struct node *) malloc (sizeof(struct node));
    
    insert(5);
    insert(4);
    insert(7);
    insert(6);
    insert(10);
    insert(12);
}


