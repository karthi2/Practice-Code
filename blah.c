#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

int main () {
    uint8_t c = 65;
    uint8_t d = 'A';

    int a[10];

    //printf("%c, %d\n", c, d);

    printf("%d %d\n", INT_MIN, INT_MAX);
    return 0;
}
